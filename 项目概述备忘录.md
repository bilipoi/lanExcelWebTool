# LAN Excel Editor 项目概述备忘录

## 项目基本信息

- **项目名称**: 局域网多人 Excel 编辑器 (LAN Excel Editor)
- **版本**: v1.4.0
- **定位**: 局域网环境下的实时协作表格编辑工具
- **技术栈**: Flask + WebSocket + Handsontable + Quill.js
- **离线包大小**: 25.7 MB

---

## 核心架构

### 后端架构

```
lan_excel_editor/
├── app.py                    # Flask 应用入口
├── config.py                 # 配置常量（用户颜色、路径等）
├── utils.py                  # 工具函数（哈希计算、安全路径）
├── requirements.txt          # Python 依赖
│
├── handlers/                 # 请求处理器
│   ├── http_handlers.py      # REST API (文件管理、附件、快照)
│   └── websocket_handlers.py # WebSocket 实时协作事件
│
├── models/                   # 数据模型
│   └── session.py            # FileSession 会话管理
│
├── services/                 # 业务逻辑服务
│   ├── file_service.py       # 文件 CRUD 操作
│   ├── style_service.py      # 单元格样式持久化 (JSON)
│   ├── type_service.py       # 单元格类型持久化 (JSON)
│   ├── attachment_service.py # 附件管理（v1.3.0 新增）
│   ├── snapshot_service.py   # 版本快照控制
│   ├── meta_service.py       # 文件元数据管理
│   ├── file_tree.py          # 文件树构建
│   └── folder_service.py     # 文件夹操作
│
├── tasks/                    # 后台任务
│   └── auto_save.py          # 自动保存（30秒间隔）
│
└── data/                     # 数据存储目录
    ├── files/                # Excel 文件 (.xlsx)
    ├── styles/               # 样式文件 (auto-hidden)
    ├── types/                # 类型文件 (auto-hidden)
    └── attachments/          # 附件存储 (auto-hidden)
```

### 前端架构

```
templates/
└── index.html                # 单页面应用 (~3200 行)
    ├── CSS 样式 (内置)
    └── JavaScript (内置)

static/
├── css/
│   ├── handsontable.full.min.css    # 表格组件样式
│   └── quill.snow.css               # 富文本编辑器样式
└── js/
    ├── handsontable.full.min.js     # 表格组件
    ├── socket.io.min.js             # WebSocket 客户端
    └── quill.min.js                 # 富文本编辑器（v1.3.0）
```

---

## 关键功能特性

### 已实现功能

| 功能 | 版本 | 描述 |
|------|------|------|
| 多页签编辑 | v1.1.0 | 浏览器式多文件标签页，同时打开多个文件 |
| 实时协作 | v1.0.0 | 多用户同时编辑，带光标指示器和用户颜色 |
| 单元格类型系统 | v1.3.0 | 6种类型：文本/日期/进度条/下拉列表/富文本/附件 |
| 看板视图 | v1.4.0 | 拖拽式卡片管理，支持状态列和展示列自定义 |
| 富文本编辑器 | v1.3.0 | Quill.js 集成，支持格式、图片、链接 |
| 附件系统 | v1.3.0 | 单元格级文件上传，50MB限制 |
| 版本快照 | v1.0.0 | 最多20个版本，支持一键回滚 |
| 格式刷 | v1.2.0 | Excel式格式复制，支持连续模式 |
| 冻结行列 | v1.2.0 | Excel式冻结窗格 |
| 查找替换 | v1.0.0 | Ctrl+F 搜索功能 |
| 撤销/重做 | v1.0.0 | Handsontable 原生支持 |
| 超链接 | v1.1.0 | LINK:path\|text 格式，支持文件跳转 |

### 单元格类型详情

1. **文本 (text)** - 默认类型，支持超链接
2. **日期 (date)** - 日期选择器，格式 YYYY-MM-DD
3. **进度条 (progress)** - 可视化进度条，输入 0-100
4. **下拉列表 (dropdown)** - 可配置选项列表
5. **富文本 (rich-text)** - Quill编辑器弹窗，支持图片粘贴转 base64
6. **附件 (attachment)** - 文件上传按钮，支持多文件

---

## 技术实现要点

### 1. 实时协作架构

**技术栈**: Flask + Flask-SocketIO + gevent (异步)

**核心机制**:
```python
# 会话隔离 - 每个文件一个 Socket.IO Room
file_sessions: Dict[str, FileSession]  # 文件路径 -> 会话对象
user_current_file: Dict[str, str]      # 用户SID -> 当前文件路径

# 冲突解决: Last-write-wins
# 通过 Room 实现文件级隔离，用户只接收当前文件的更新
```

### 2. 多页签状态管理

```javascript
// 前端状态管理
openFileTabs = {
  'file1.xlsx': {
    sheetsData: {...},           // 表格数据
    currentSheet: 'Sheet1',      // 当前工作表
    cellStyles: {...},           // 样式数据
    cellTypes: {...},            // 类型数据
    readonly: false,
    myColor: '#3498db',
    users: [...]                 // 在线用户列表
  }
}
activeFilePath = 'file1.xlsx'      // 当前激活页签

// 使用 getter/setter 实现向后兼容
Object.defineProperty(window, 'sheetsData', {
  get: () => openFileTabs[activeFilePath]?.sheetsData
})
```

### 3. 数据持久化策略

**三层持久化**:

1. **实时持久化** (JSON文件)
   - 单元格样式 → `data/styles/*.styles.json`
   - 单元格类型 → `data/types/*.types.json`
   - 每次变更立即写入

2. **Excel文件保存**
   - 保存时写入 `data/files/*.xlsx`
   - MD5哈希检测内容变更

3. **版本快照** (自动备份)
   - 最多20个版本，自动清理最旧的
   - 包含: Excel文件 + 样式 + 类型 + 附件
   - 触发: 手动保存、自动保存(30秒)、版本恢复

### 4. WebSocket事件体系

**客户端 → 服务器**:
- `join_file` / `leave_file` - 加入/离开文件
- `cell_edit` - 单元格编辑
- `cell_select` / `cell_deselect` - 选区变更
- `structure_change` - 增删行列
- `cell_style_change` - 样式变更
- `cell_type_change` - 类型变更
- `save_file` - 手动保存

**服务器 → 客户端**:
- `file_opened` / `file_data_updated` - 文件数据
- `cell_updated` / `structure_updated` - 更新广播
- `user_joined` / `user_left` - 用户状态
- `save_result` / `file_saved` - 保存确认

### 5. 看板视图实现

**核心机制**:
```javascript
// 数据转换
statusMap = new Map()  // 状态值 -> 卡片数组

// 根据状态列分组渲染
// 拖拽后更新单元格值 → emit cell_edit → 广播更新所有客户端
```

**功能**:
- 视图切换: 表格视图 ↔ 看板视图
- 状态列选择: 任意列作为分组依据
- 展示列自定义: 多选标签式选择器
- 拖拽交互: HTML5 Drag and Drop API

### 6. 附件系统

**存储结构**:
```
data/attachments/{file_hash}/{sheet}/{row}_{col}/
  ├── {uuid1}_{original_filename}
  └── {uuid2}_{original_filename}
```

**特性**:
- 50MB 单文件限制
- 按单元格隔离存储
- 支持多文件上传/下载/删除
- 随版本快照备份和恢复
- WebSocket 广播变更

### 7. 安全机制

**路径安全**:
```python
def safe_join(base: str, *parts: str) -> Optional[str]:
    path = os.path.realpath(os.path.join(base, *parts))
    if not path.startswith(os.path.realpath(base)):
        return None  # 路径遍历攻击防护
    return path
```

**只读模式**:
- 元数据标记 `readonly: true`
- 前端禁用所有编辑操作
- 后端 API 层二次验证

**文件上传**:
- 强制 `.xlsx` 扩展名
- 文件名清理（防止路径遍历）
- 大小限制 50MB

---

## 离线部署

### 离线包内容

- **文件名**: `lan_excel_editor_offline_v1.4.0.zip`
- **大小**: 25.7 MB
- **文件数**: 57个文件
- **Python依赖**: 30个 wheel 包
- **前端资源**: 已本地化，无需CDN

### 部署步骤

1. 解压离线包到任意目录
2. 安装 Python 3.9+ (添加到PATH)
3. 双击 `start.bat` 启动服务
4. 访问 `http://localhost:5000`
5. 局域网其他设备通过服务器IP访问

---

## 项目统计

| 指标 | 数值 |
|------|------|
| Python 代码行数 | ~2000 行 |
| 前端代码行数 | ~3200 行 |
| 核心服务模块 | 8 个 |
| WebSocket 事件类型 | 16 个 |
| REST API 端点 | 15+ 个 |
| 单元格类型 | 6 种 |

---

## 最新更新 (v1.4.0)

1. ✅ **看板视图** - 拖拽式任务管理
2. ✅ **富文本编辑器** - Quill.js 集成
3. ✅ **单元格附件** - 文件上传功能
4. ✅ **单元格类型系统** - 6种可扩展类型
5. ✅ **展示列自定义** - 标签式选择器
6. ✅ **行删除保存修复** - 结构变更检测

---

## 技术亮点总结

1. **文件级会话隔离** - Socket.IO Room 实现多文件独立编辑
2. **复杂状态管理** - 前端多页签状态，支持实时切换
3. **可扩展类型架构** - 自定义渲染器和编辑器
4. **富文本集成** - Quill.js 嵌入，图片 base64 处理
5. **数据可视化** - 看板视图与表格数据双向同步
6. **完整离线支持** - 所有依赖本地化，零互联网依赖
7. **版本控制** - 自动快照，一键回滚
8. **安全防护** - 路径遍历防护、只读模式、上传限制

---

**创建时间**: 2026-03-26
**最后更新**: v1.4.0
**状态**: 功能完整，生产可用
